'use strict';

/**
 * Get all images.
 *
 * @param {Object} req The request object
 * @return {Promise} The promise
 */
function getImages(req) {
  return Promise.resolve('stub');
}

/**
 * Get an image with id.
 *
 * @param {Object} req The request object
 * @return {Promise} The promise
 */
function getImage(req) {
  return Promise.resolve('stub');
}

/**
 * Get all images for a city.
 *
 * @param {Object} req The request object
 * @return {Promise} The promise
 */
function getImagesForCity(req) {
  return Promise.resolve('stub');
}

/**
 * Get all images for a place.
 *
 * @param {Object} req The request object
 * @return {Promise} The promise
 */
function getImagesForPlace(req) {
  return Promise.resolve('stub');
}

/**
 * Create an image.
 *
 * @param {Object} req The request object
 * @return {Promise} The promise
 */
function createImage(req) {
  return Promise.resolve('stub');
}

/**
 * Update an image.
 *
 * @param {Object} req The request object
 * @return {Promise} The promise
 */
function updateImage(req) {
  return Promise.resolve('stub');
}

/**
 * Delete an image.
 *
 * @param {Object} req The request object
 * @return {Promise} The promise
 */
function deleteImage(req) {
  return Promise.resolve('stub');
}

module.exports = {
  getImages, getImage, getImagesForCity, getImagesForPlace, createImage, updateImage, deleteImage
};
